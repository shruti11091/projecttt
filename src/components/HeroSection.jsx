import React from 'react';
import './HeroSection.css'; // Import CSS for HeroSection

const HeroSection = () => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h1>Elevate Your Print Business</h1>
        <p>Connect, create, and sell with ease.</p>
        <button className="btn">Start Your Free Trial</button>
      </div>
    </section>
  );
};

export default HeroSection;
